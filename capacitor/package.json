{
  "name": "eel-shock-native",
  "private": true,
  "version": "1.0.0",
  "description": "Native iOS/Android wrapper for Eel Shock. It packages the shared dist/app build and is not part of the browser build.",
  "scripts": {
    "stage": "node sync-app.mjs",
    "sync:built": "npm run stage && cap sync",
    "sync": "npm --prefix .. run build:app && npm run sync:built",
    "open:ios": "npm run sync && npx cap open ios",
    "open:android": "npm run sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0",
    "@capacitor/ios": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  }
}
